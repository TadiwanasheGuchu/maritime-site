// Edge-safe constants (no next/headers import) so proxy.ts can use them.

/** Dev-only fallback session cookie, used before Supabase is configured. */
export const SESSION_COOKIE = "monarch_session";
